import 'package:flutter/material.dart';

class MoviesAppView extends StatelessWidget {
  const MoviesAppView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}