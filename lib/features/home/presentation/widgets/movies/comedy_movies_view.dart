import 'package:flutter/material.dart';

class ComedyMoviesView extends StatelessWidget {
  const ComedyMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "comedy movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
