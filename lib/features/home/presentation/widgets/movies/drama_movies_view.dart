import 'package:flutter/material.dart';

class DramaMoviesView extends StatelessWidget {
  const DramaMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "drama movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
