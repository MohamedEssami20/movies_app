import 'package:flutter/material.dart';

class CrimeMoviesView extends StatelessWidget {
  const CrimeMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "comedy movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
