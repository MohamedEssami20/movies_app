import 'package:flutter/material.dart';

class AdventureMoviesView extends StatelessWidget {
  const AdventureMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "adventure movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}