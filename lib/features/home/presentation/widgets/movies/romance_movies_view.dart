import 'package:flutter/material.dart';

class RomanceMoviesView extends StatelessWidget {
  const RomanceMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "romance movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}