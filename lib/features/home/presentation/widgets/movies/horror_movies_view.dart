import 'package:flutter/material.dart';

class HorrorMoviesview extends StatelessWidget {
  const HorrorMoviesview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "horror movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}