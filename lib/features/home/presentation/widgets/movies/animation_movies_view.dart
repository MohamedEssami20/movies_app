import 'package:flutter/material.dart';

class AnimationMoviesView extends StatelessWidget {
  const AnimationMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "animation movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}