import 'package:flutter/material.dart';

class FamilyMoviesView extends StatelessWidget {
  const FamilyMoviesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "family movies",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}